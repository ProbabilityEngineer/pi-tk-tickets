# pi-tickets (deprecated)

`pi-tickets` has been renamed to [`pi-tk-tickets`](https://www.npmjs.com/package/pi-tk-tickets) to distinguish it from unrelated ticket extensions.

Install the replacement:

```bash
pi install npm:pi-tk-tickets
```
